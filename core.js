export function marketFrom(symbol='',exchange=''){const s=String(symbol).toUpperCase(),e=String(exchange).toUpperCase();if(/\.(TW|TWO)$/.test(s)||/TWSE|TPEX|TAIWAN|TAI/.test(e))return'TW';if(/\.(SS|SZ)$/.test(s)||/SHANGHAI|SHENZHEN|BEIJING|SSE|SZSE|BSE/.test(e))return'CN';return'US'}
export function cleanSymbol(s=''){return String(s).toUpperCase().replace(/\.(TW|TWO|SS|SZ)$/,'')}
export function symbolCandidates(market,symbol){const s=cleanSymbol(symbol);if(market==='TW')return[`${s}.TW`,`${s}.TWO`,`${s}:TPE`,s];if(market==='CN'){if(/^6|^68/.test(s))return[`${s}.SS`,`${s}:SHH`,s];if(/^8|^4/.test(s))return[`${s}.BJ`,s];return[`${s}.SZ`,`${s}:SHZ`,s]}return[s]}
export function tradingViewSymbol(row){const m=row.market||marketFrom(row.symbol,row.exchange),s=cleanSymbol(row.symbol),e=String(row.exchange||'').toUpperCase();if(m==='TW')return`${e.includes('TPEX')?'TPEX':'TWSE'}:${s}`;if(m==='CN')return`${(/^6|^68/.test(s)||e.includes('SSE'))?'SSE':(/^8|^4/.test(s)||e.includes('BSE'))?'BSE':'SZSE'}:${s}`;const ex=e.includes('NYSE')?'NYSE':e.includes('AMEX')?'AMEX':'NASDAQ';return`${ex}:${s}`}
export function normalizeRow(r,source='remote'){const raw=r.symbol||r.displaySymbol||r.code||r.ticker||'',exchange=r.exchangeShortName||r.exchange||r.mic||r.stockExchange||'',market=marketFrom(raw,exchange);return{market,symbol:cleanSymbol(r.displaySymbol||raw),providerSymbol:raw,name:r.name||r.companyName||r.description||r.instrument_name||cleanSymbol(raw),en:r.companyName||r.description||r.name||'',exchange:exchange||market,industry:r.sector||r.industry||r.type||'股票',currency:r.currency||({TW:'TWD',CN:'CNY',US:'USD'}[market]),source,tradingView:tradingViewSymbol({market,symbol:cleanSymbol(raw),exchange})}}
export function scoreFund(f){let total=50,s=[],r=[];if(f.roe!=null){if(f.roe>=20){total+=15;s.push('ROE 優秀')}else if(f.roe<8){total-=10;r.push('ROE 偏低')}}if(f.growth!=null){if(f.growth>=15){total+=15;s.push('營收成長強')}else if(f.growth<0){total-=12;r.push('營收衰退')}}if(f.pe!=null){if(f.pe>0&&f.pe<=25){total+=10;s.push('估值相對合理')}else if(f.pe>60){total-=12;r.push('估值偏高')}}if(f.debt!=null){if(f.debt<45){total+=10;s.push('負債較穩健')}else if(f.debt>70){total-=10;r.push('負債偏高')}}total=Math.max(0,Math.min(100,Math.round(total)));return{total,rating:total>=85?'優秀':total>=70?'良好':total>=55?'中性':'注意',strengths:s,risks:r}}
